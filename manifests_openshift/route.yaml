apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: route
  labels:
    type: external
spec:
  port:
    targetPort: 5001
  to:
    kind: Service
    name: svc
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
