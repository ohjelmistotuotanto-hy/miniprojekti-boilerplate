kind: ImageStream
apiVersion: image.openshift.io/v1

metadata:
  name: app
spec:
  lookupPolicy:
    local: false
  tags:
    - name: latest
      from:
        kind: DockerImage
        name: dockerhub-image:latest
      importPolicy:
        scheduled: true
      referencePolicy:
        type: Local
